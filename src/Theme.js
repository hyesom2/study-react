const theme = {
  // ${({ theme }) => theme.colors.컬러이름};
  colors: {
    black: '#000',
    white: '#fff',
  },

  // ${({ theme }) => theme.fs.size};
  fs: {
    10: '10px',
    11: '11px',
    12: '12px',
  },

  // ${({ theme }) => theme.margin.size};
  margin: {
    10: '10px',
  }
}

export default theme;